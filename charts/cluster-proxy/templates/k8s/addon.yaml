{{- if not .Values.kubeconfigSecretName }}

{{- $restpl := $.Files.Get "common/clustermanagementaddon.yaml" -}}
{{ tpl $restpl $ }}
---
{{- $restpl := $.Files.Get "common/managedproxyconfiguration.yaml" -}}
{{ tpl $restpl $ }}

{{- end }}
